#include "libft.h"

char *strnstr(const char *big, const char *little, size_t len)
{
    if (ft_strlen(little) == 0)
    {
        return (big);
    }

    char c;
    char sc;

    while (len--)
    {
        if(*ptr_big++ ==
    }
}